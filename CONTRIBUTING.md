# Contributing

Keep contributions small, verifiable, and honest.

## Setup

```bash
python -m pip install -e .[dev]
python -m pytest -q
```

## Before Opening a PR

- Keep the change focused on one behavior, doc gap, or test gap.
- Include a verification command or manual check.
- Update docs when CLI behavior or report output changes.
- Call out risks instead of overstating safety.
