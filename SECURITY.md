# Security

This repo is a public learning project. Do not include secrets, tokens, private
repository URLs, customer data, or personal credentials in contribution plans,
examples, issues, or pull requests.

If you accidentally commit sensitive data, rotate the credential first, then
open an issue describing the cleanup needed without repeating the secret.
